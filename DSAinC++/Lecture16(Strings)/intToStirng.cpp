#include <iostream>
#include <string>
using namespace std;
int main()
{
    int x = 123455;
    string s = to_string(x);
    cout << s << endl;
    //typecasting of srings is not valid, we cannot write ,(string)x, in above question
}