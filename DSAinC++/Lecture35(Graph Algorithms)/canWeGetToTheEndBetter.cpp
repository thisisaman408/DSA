#include<iostream>
using namespace std;

int main(){
    int m,n;
    cin>>m>>n;
    char arr[m][n];
    for(int i = 0; i < n;i++) cin>>arr[i][j];
    int x,y;
    cin>>x>>y;
    int slowX = x;
    int slowY = y;

    int fastX = x;
    int fastY = y;
    while(slow != fast){
        if(arr[slow][fast])
    }
}